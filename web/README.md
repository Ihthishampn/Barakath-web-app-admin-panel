# Barkath — User Website

Customer-facing web client (Next.js). Placeholder for a later milestone —
scaffolded here so all three clients (admin, web, app) share one repo and the
`packages/shared` types + `firebase/` rules.
