# Brand assets

Drop the real prototype assets here — they are referenced by name and used
automatically (asset-preservation rule):

- `logo.png` — the Barkath logo (the prototype references `images/logo.png`).
  Until it's provided, `<BrandLogo>` falls back to a wordmark. It is **not**
  silently replaced with a fabricated logo.

Other missing prototype assets to add when available:
`_ds/ecom-barakath-.../tokens/{colors,fonts,scale}.css`, `_ds_bundle.js`, and
category illustrations (`perfume.png`, `book.png`, `clothing.png`, `islamic.png`).
